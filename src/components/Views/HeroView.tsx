"use client";

import Hero from "@components/Hero/Hero"; // Adjust the path based on your project structure

const HeroView = () => {
  return (
    <div>
      <Hero />
    </div>
  );
};

export default HeroView;
